server:
  port: 8080
  contextPath: /Tracker
  
spring:
  datasource:
    url: jdbc:mysql://localhost/project_tracker
    username: root
    password: whosyourdaddy
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf.cache: false
  
security:
  basic:
    enabled: false

logging:
 level:
  org:
    springframework:
      security: DEBUG